# Content-monitoring-Flagging-system
This project is a backend system that ingests content, matches it against user-defined keywords, and generates flags for review.  It also supports reviewer decisions and ensures that previously marked irrelevant flags do not reappear unless the content has been updated.
